name: Daily Prayer Sender

on:
  # יריץ את התהליך מיד ברגע שתעשי Push לקוד
  push:
    branches:
      - main
  
  schedule:
    # מוגדר ל-16:45 UTC (שהם 18:45 שעון ישראל)
    # ימים: ראשון עד חמישי (0-4 ב-Cron)
    - cron: '45 16 * * 0,1,2,3,4'
  
  # מאפשר הרצה ידנית מכפתור ה-Actions ב-GitHub
  workflow_dispatch:

jobs:
  send-daily-prayer:
    name: Send Daily Prayer
    runs-on: ubuntu-latest

    steps:
      - name: Wake up Render service (ping)
        run: |
          echo "Pinging Render service to wake it up..."
          curl --silent --output /dev/null \
               --connect-timeout 60 \
               --retry 3 \
               --retry-delay 10 \
               --retry-connrefused \
               https://run-prayers-apirun-prayers-api.onrender.com/health || true
          echo "Waiting 20s for service to fully wake up..."
          sleep 20

      - name: Send daily prayer
        run: |
          echo "Sending daily prayer..."
          RESPONSE=$(curl --silent \
                          --show-error \
                          --fail \
                          --connect-timeout 60 \
                          --max-time 120 \
                          --retry 3 \
                          --retry-delay 15 \
                          --retry-connrefused \
                          --write-out "\n%{http_code}" \
                          --request POST \
                          "https://run-prayers-apirun-prayers-api.onrender.com/api/send_prayer?target_list=default")
          
          HTTP_STATUS=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)
          
          echo "HTTP Status: $HTTP_STATUS"
          echo "Response: $BODY"
          
          if [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "✅ Daily prayer sent successfully!"
          else
            echo "❌ Failed to send prayer. HTTP status: $HTTP_STATUS"
            exit 1
          fi